# Input rules for KToshiba
# By default, this rule will change the group of the
# "Toshiba input device" to "users" and its permissions
# to read-write, however, you might want to change this
# to the group "input" for example.
SUBSYSTEMS=="input", ATTRS{name}=="Toshiba input device", MODE="0660", GROUP="users"
